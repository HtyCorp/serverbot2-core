package com.admiralbot.appdaemon.model;

import com.admiralbot.framework.common.ApiRequestInfo;

@ApiRequestInfo(order = 0, name = "StartApp", numRequiredFields = 0, description = "Start the app configured on this server")
public class StartAppRequest {
    // EMPTY
}
